package ske.part.partsregister.interfaces;

import static com.google.common.collect.Maps.newHashMap;

import java.util.List;
import java.util.Map;

import ske.eventsourcing.event.Event;

public class PartViewStore {

    private final Map<String, List<Event>> partViewmodell = newHashMap();

    public PartDTO hentPart(String partId) {

    }

    public PartViewStore(PartModell)
}
